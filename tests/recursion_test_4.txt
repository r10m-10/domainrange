node = ('+', '1', ('*', '3', 'x'))
expected terms = [(1.0, None, None), (3.0, 'x', 1.0)]

----------LEVEL 1----------
1. op = '+', left = '1', right = ('*', '3', 'x')
2. left = simplify_initial(left), right = simplify_initial(right)

----------LEVEL 2 (LEFT)----------
1. type(node)==str and it is a number so we simply return 1.0

----------LEVEL 2 (RIGHT)----------
1. op = '*', left = '3', right = 'x'
2. left = simplify_initial(left), right = simplify_initial(right)

----------LEVEL 2.1 (LEFT)----------
1. type(node)==str and it is a number so we simply return 3.0

----------LEVEL 2.1 (RIGHT)----------
1. type(node)==str and it is a string so we simply return 'x'

----------LEVEL 2 (RIGHT)----------
3. op = '*', left = 3.0, right = 'x'
4. as op = '*', we enter the condition where we run the flatten and stuff
5. fl = flatten((op,left,right), op) => flatten(('*', 3.0, 'x'), '*') => fl = [3.0, 'x']
6. for i in fl: we do to_term so for the first iteration we call to_term(3.0) which should return (3.0, None, None)
BUG FOUND: in step 6, to_term does not expect a float to be an input so either we have to change simplify_initial to return
the node itself when type of node is string or wither we must handle floats in to_term, both are simplt fixes but need to look
at the bigger picture for simplify_initial as it is the main recursive function.
7. now when bug handled, the first iteration returns (3.0, None, None) and we append that to a list called terms.
8. second iteration will be to_term('x') which will sucessfully return (1.0, 'x', 1.0)
9. Now from currently unwritten logic we solve and recombine it in such a way that this level returns ('*', '3', 'x')

----------LEVEL 1----------
3. op = '+', left = 1.0, right = ('*', '3', 'x')
4. as op = '+', we enter the condition where we run the flatten and stuff
5. fl = flatten((op,left,right), op) => flatten(('+', 1.0, ('*', '3', 'x')), '+') => fl = [1.0, ('*', '3', 'x')]
6. for i in fl: we do to_term so for the first iteration we call to_term(1.0) which should return (1.0, None, None)
BUG FOUND: Same as previous
7. now when bug handled, the first iteration returns (1.0, None, None) and we append that to a list called terms.
8. second iteration will be to_term(('*', '3', 'x')) which will sucessfully return (3.0, 'x', 1.0)
9. therefore, terms=[(1.0, None, None), (3.0, 'x', 1.0)] which matches the expected_terms so the recursion logic is correct except one bug
9. Now from currently unwritten logic we solve and recombine it in such a way that this level returns ('+', '1', ('*', '3', 'x'))
which is correct because 1+3x cannot be further simplified and I took a bad example but I found a bug so it is all good.

IMPORTANT: The bug that I have pointed out is non-existent as in my current code itself I have already fixed it by doing:
if type(left)==float:
    left = convert_to_str(left)
if type(right)==float:
    right = convert_to_str(right)
flat = flatten((op, left, right), op)
which means I already knew that before calling flatten, I need to convert any floats to string. My smarts are too smart.